export interface ProductSpec {
  model: string;
  motorPower: string;
  ratedCurrent: string;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
}
